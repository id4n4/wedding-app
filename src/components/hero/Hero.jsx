import { Metric } from '@tremor/react'

export const Hero = () => {
  return (
    <section className='min-h-[500px] w-full bg-primary'>
      <div>
        <Metric>Agosto</Metric>
      </div>
    </section>
  )
}
